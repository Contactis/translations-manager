.container-fluid
  .row
    .col-xs-12
      h1 Translations
      p
        | Here translators can translate phrases from default language for the project (in this case: {{ vm.currentProject.defaultLanguage.nameNative }}, {{vm.currentProject.defaultLanguage.ietfCode}}) to another language available.
        br
        strong Translations will be automatically saved after hit enter (or focus off).


  .row
    .col-md-12
      .panel.panel-info(data-ng-if="vm.displayedCollection.length == 0")
        .panel-heading
          | No translations available for current project
        .panel-body
          | Programmers team need to provide translation strings.

      .card(data-ng-if="vm.displayedCollection.length > 0")
        //- .card-header
        //-   .card-title
        //-     .title Translations
        .card-body
          table.table.table-striped(search-watch-model="vm.query" st-safe-src="vm.tableData" st-table="vm.displayedCollection")
            thead
              tr
                th Group
                th
                  select.form-control(data-ng-model="vm.translateLanguage"
                  data-ng-options="opt.nameNative for opt in vm.allLanguages"
                  data-ng-change="vm.updateLanguage(vm.translateLanguage)")
                th Default language ({{ vm.currentProject.defaultLanguage.nameNative }})
                th Context description ({{ vm.currentProject.defaultLanguage.nameNative }})
                th
                  div(data-tr-access-level="vm.accessLevels.programmer") {{ 'GENERAL.MODIFY' | translate }}
            tbody
              tr(data-ng-repeat="singleRow in vm.displayedCollection")
                td {{singleRow.keyString}}
                //- if for default language
                td(data-ng-hide="vm.isTranslationForDefaultLanguage()")
                  tr-edit-table(translate-val="singleRow.translations[1]" translate-object="singleRow")
                td(data-ng-show="vm.isTranslationForDefaultLanguage()")
                  tr-edit-table(translate-val="singleRow.translations[0]" translate-object="singleRow")
                td {{singleRow.translations[0].translatedPhrase}}
                td {{singleRow.translations[0].description}}
                td(data-tr-access-level="vm.accessLevels.programmer")
                  button.btn.btn-xs.btn-danger(data-ng-click="vm.deleteTranslation($index)") {{ 'GENERAL.DELETE' | translate}}
