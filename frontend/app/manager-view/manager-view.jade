tr-top-bar(filters='filters' context-menu="contextMenu")

table.table.table-striped(st-table="rowCollection")
  thead
  tr
    th(st-sort="keyString") Group
    th Context description
    th English (en-GB)
    th English (en-US)
    th Polski (pl-PL)
  tbody
  tr(data-ng-repeat="singleRow in tableData")
    td {{singleRow.keyString}}
    td mocked1
    td mocked2
    td mocked3
    td mocked4
